<!DOCTYPE html>
<html lang="fr">
 
<head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible", content="IE=edge,chrome=1" />
    <title><%= title %></title>

    <link rel="stylesheet" href="/static/style.css" type="text/css" media="screen">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/static/client.js"></script>
    <script type="text/javascript">
        $(function(){
            $('#gameBoard').find('a').click(function(){

                console.log(this,$(this));
            });
        });

        var socket = io.connect(location.host);
        socket.emit('new');
        socket.on('init', function(data){
            updateBoard(data.gameState);
        });
    </script>
</head>

<body>
    <h1><%= title %></h1>
    <p id="info">Game is loading...</p>
    <p id="gameBoard">
        <a data-position="[0,0]"></a>
        <a data-position="[0,1]"></a>
        <a data-position="[0,2]"></a>

        <a data-position="[1,0]"></a>
        <a data-position="[1,1]"></a>
        <a data-position="[1,2]"></a>
        
        <a data-position="[2,0]"></a>
        <a data-position="[2,1]"></a>
        <a data-position="[2,2]"></a>
    </p>
</body>

</html>